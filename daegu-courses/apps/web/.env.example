# Server-side API base (Next.js Route Handlers, Server Components)
API_BASE_URL=http://localhost:3000
# Browser-side base (use full origin if API runs on a different host in prod)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Kakao OIDC — REST API key from https://developers.kakao.com/console/app
NEXT_PUBLIC_KAKAO_CLIENT_ID=

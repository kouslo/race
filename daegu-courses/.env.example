DATABASE_URL=postgres://postgres:postgres@localhost:5432/daegu_courses

# Daegu library CMS JSON API origin (no trailing slash).
# Used by adapter daegu-library-v1. Discover the value via the page's
# Network tab — look for a request ending in /<hmpgUid>/courses.
DAEGU_LIBRARY_CMS_API_HOST=

